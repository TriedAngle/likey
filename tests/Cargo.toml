[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
storage = { path = "../storage" }
algos = { path = "../algos" }
like = { path = "../like" }
engine = { path = "../engine" }
clap = { version = "4.5.53", features = ["derive"] }
csv = "1.3"
